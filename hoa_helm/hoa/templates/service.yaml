apiVersion: v1
kind: Service
metadata:
  name: {{ include "hoa.fullname" . }}
  labels:
    {{- include "hoa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hoa.selectorLabels" . | nindent 4 }}
